# AI-Document-Analyzer
A Python project that reads local text files and PDFs, uses an LLM to generate high-level summaries, and synthesizes them into a structured briefing pack. Built to practice core Python concepts (functions, loops, lists, dictionaries, file handling) and AI-assisted text analysis.
